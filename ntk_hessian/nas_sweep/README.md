Example Neural Architecture Search — MNIST MLP Sweep

**Premise:** Because MPDOK's v9 Fortran/cuBLAS engine can train an MNIST MLP in <100 ms,
we can afford to *brute-force* the architecture space that normally costs researchers days.

This notebook is run with dependent directories and will need to be adjusted on another machine (it might not run). But it is provided as an example of what is possible.
